import { ConvexReactClient } from "convex/react";

// Use the production URL directly for deployment
const convex = new ConvexReactClient("https://optimistic-bear-863.convex.cloud");

export default convex;
